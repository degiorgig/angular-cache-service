{
  "name": "ng-cache-service",
  "private": true,
  "version": "0.0.0",
  "description": "A proof of concept about Angularjs service caching",
  "repository": "https://github.com/leader80/angular-cache-service",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "jasmine-core": "^2.3.4",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
